import { LineBreakerPipe } from './line-breaker.pipe';

describe('LineBreakerPipe', () => {
  it('create an instance', () => {
    const pipe = new LineBreakerPipe();
    expect(pipe).toBeTruthy();
  });
});
