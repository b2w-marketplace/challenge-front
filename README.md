b2w-challenge Felipe Antonio Alves Costa

Projeto feito em React.
Usando Sass com o compilador Scout.

Para rodar use:
npm install e npm start

Para rodar na pasta bin/build: 

npm instal -g pushstate-server
pushstate-server build


